# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.output/
.vinxi/
.tanstack/
.vite/

# TypeScript
*.tsbuildinfo

# SQLite Database (lives locally, never committed) — DATABASE_PATH = ./data/pulse.db
data/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Env / secrets — managed via `keyring exec` (Keychain populated from 1Password by `keyring sync`); never committed
.env
.env.*
!.env.example

# direnv
.envrc

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Test / coverage
coverage/
.nyc_output/

# Local tooling (not part of the project)
.pi/

# Claude Code worktrees — linked checkouts created under .claude/, never committed
.claude/worktrees/

# Caches
.cache/
.turbo/
apps/web/src/routeTree.gen.ts
